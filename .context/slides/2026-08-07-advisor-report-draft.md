# Omni-Anything 导师汇报幻灯片草稿

更新日期：2026-08-18（学术表述版 v4）
范围：12 页 · 标题 / 项目总览大图 / 研究背景 / 任务形态 / 问题与挑战 / 研究内容总览 / RP1 / RP1 实验 / RP2 / RP2 实验 / RP3 / 交付物
读者：双用——导师 + 企业合作方（JiuwenSwarm 团队）；保持学术中文底色，仅实验页「收益含义」行用价值语言；不假设读者熟悉双工模型
口径：RP1 进行中·有实验支撑；RP2 进行中·有实验支撑（收敛稿 `.context/ideas/static-specialization.md` 2026-08-18 版）；RP3 立项设想
生成：`make-2026-08-07-advisor-pptx.py`（`~/.venvs/slides/bin/python` 运行）→ `omni-anything-proposal.pptx`（对外文件名不带日期）；每页讲法在 pptx 演讲备注中
配图：第 2 页 `figure-overview-jiuwenswarm.png`（项目总览大图）；第 4 页 `figure-duplex-workload-cn.png`（任务形态）；第 7/9/11 页右侧各配研究点示意图 `figure-rp{1,2,3}-idea.png`（行块窄版 + 右图版式）

---

## 第 1 页｜标题

# Omni-Anything

## 为智能体提供前台双工交互能力的推理服务系统

**支撑「前台双工交互 + 后台智能体协同」负载的高效推理服务。**

### 怎么讲

> 智能体的推理与工具能力已经成熟，但与用户的交互仍以一问一答为主。本项目为智能体配备双工交互前台——前台模型持续接收输入、实时生成输出，后台智能体异步执行任务、结果实时写回——并研究支撑这一负载的推理服务系统。

---

## 第 2 页｜项目总览（大图）

# 为 agentic 系统构建实时双工交互底座

导语（图上方两行）：**JiuwenSwarm 等 agentic 系统已经能替人完成复杂任务，但触达用户仍以文字问答为主。本项目为它们构建实时语音双工交互的推理底座：这层系统决定双工交互能否规模化上线，也决定后台智能体的成果能否被及时说出口。**

配图：`figure-overview-jiuwenswarm.png`——一张图讲清楚项目做什么：
- 上半（空间结构）：用户/真实世界 ⇄（网络）⇄ **本项目 · Omni 交互运行时**（前台双工模型 + 会话状态）⇄ JiuwenSwarm 多智能体协作系统（Leader/Teammates、技能自进化、工具调用）；任务异步派发、结果写回
- 下半（时间结构）：前台刚性周期网格（每格一个 deadline）+ 后台弹性任务条 + 结果注入箭头
- 三个标注写明「为什么这是研究点」：① 状态只增不减，显存必先于算力耗尽 → 研究点 1；② 开销压过计算：通用引擎不识别固定形状 → 研究点 2；③ 四环节各自留余量，写回时机无人统筹 → 研究点 3

### 怎么讲

> 一张图先立全局：左边是持续说话、随时打断的用户；中间是本项目——前台双工模型和它的交互运行时，让智能体「听得见、说得出」；右边是 JiuwenSwarm 这样的多智能体系统，在后台组队干活，结果异步写回对话。下半的时间轴是这个负载的本质：前台是刚性的周期 deadline，后台是弹性的异步任务。图上三个标注就是三个研究点的由来——状态装不下、每周期跑不轻、链路没人统筹。带着这三个问题往下看，后面每一页都有落点。

---

## 第 3 页｜研究背景

# 智能体双工交互已进入产品，系统层研究刚刚起步

- **交互范式演进**：智能体的推理与工具调用能力已趋成熟，但其与用户的交互仍以「请求—响应」为主。新一代产品为智能体配备了双工交互前台：前台模型持续接收输入、同时实时生成输出（如实时语音），后台智能体异步执行任务并将结果写回对话。OpenAI 与字节跳动今年相继发布此类产品。
- **研究现状与空白**：上述产品的服务侧方案均未公开；学术界对该负载的系统研究刚刚起步，「前台双工交互 + 后台结果写回」的服务问题仍缺乏公开的系统性方案。
- **研究目标**（强调）：为智能体的前台双工交互构建推理服务系统：在实时性约束下，最大化单位 GPU 资源的并发会话承载能力。

### 怎么讲

> 智能体的能力已经成熟，交互形态正在升级：新一代产品给智能体配上了双工交互前台，前台持续听说，后台智能体干活、结果实时写回，OpenAI 和字节的产品已经验证了这一方向。但服务侧方案都不公开，学术研究刚刚起步。本项目的研究目标是：在实时性约束下，最大化单位 GPU 资源可承载的并发会话数。

---

## 第 4 页｜任务形态

# 双工模型的任务形态：周期性连续请求，每周期须按时完成

配图：`figure-duplex-workload-cn.png`（两种任务形态对比：A · 聊天 / 智能体请求——回合制、回合间有空闲、状态可在空闲时换出；B · 双工交互会话——周期性 + 持续在线、每 80 ms 至 2 s 一个 deadline、常驻 KV 逐帧增长且没有空闲可供换出）

顶部一句话导语：**双工模型不是「一次请求、一次回答」：会话期间模型以固定周期持续运转，每个周期都要在 deadline 前完成「接收输入 → 生成输出」的一轮计算，同时后台智能体的任务调用与结果写回穿插其中。**

### 怎么讲

> 先建立任务形态的直觉：普通聊天或智能体请求是回合制的，回合之间有空闲，系统可以利用空闲腾挪状态。双工会话完全不同——它是周期性的连续请求，每个周期都有硬性时限，会话全程没有空闲，状态必须常驻且持续增长。再叠加一层：前台在跟用户实时交互的同时，还会调用后台智能体或工具，结果要写回对话。后面的三个研究点，都建立在这个任务形态之上。

---

## 第 5 页｜问题与挑战

# 现有推理系统与持续交互负载的根本失配

- **挑战 1｜状态容量** → 对应研究点 1
  会话状态常驻且持续增长：上下文状态（KV 缓存）随会话时长单调增长，且每个交互周期都必须访问、无法换出。实测表明，GPU 计算远未饱和时，会话已因显存耗尽而失败。
- **挑战 2｜执行效率** → 对应研究点 2
  周期计算粒度小、频率高：每个交互周期的有效计算量很小，而通用引擎将其视为动态负载，调度、内核启动与内存规划等固定开销按周期重复支付，占比显著。
- **挑战 3｜链路协同** → 对应研究点 3
  端到端实时性由多个环节共同决定：一次双工交互跨越网络传输、前台模型推理、后台智能体调用与客户端播放，各环节由独立子系统按局部目标优化、彼此状态不可见，实时时限只能靠各环节独立预留余量保障。

### 怎么讲

> 这类负载与现有系统存在三项根本失配：其一，会话状态常驻且持续增长，显存容量先于计算能力成为瓶颈；其二，每周期计算量小而动态执行开销大；其三，端到端实时性横跨传输、推理、后台调用与播放多个环节，而各环节孤立优化、互不知情。三项挑战分别由三个研究点针对性解决——和总览大图上的三个标注一一对应。

---

## 第 6 页｜研究内容总览

# 三个研究点

横幅：**总体目标：在实时性约束下，最大化单位 GPU 资源的并发会话承载能力**

| 研究点 1 | 研究点 2 | 研究点 3 |
| --- | --- | --- |
| 会话状态容量扩展 | 周期执行静态特化 | 端到端链路联合调度 |
| 利用交互周期的可预测性，以闲置传输带宽换取等效显存容量，无损保全上下文 | 利用周期任务输入形状固定的特性，将逐周期动态调度转化为静态执行计划的直接重放 | 将网络传输、前台推理、后台智能体调用与播放纳入同一控制环，共同守护端到端实时性 |
| ● 进行中，有实验支撑 | ● 进行中，有实验支撑 | ○ 立项设想 |

### 怎么讲

> 项目的总体目标是在实时性约束下，用同样的 GPU 资源承载更多并发会话。研究点一扩展状态容量、研究点二消除动态执行开销，两者都已进入实验验证并各有一页实验结果；研究点三把端到端链路纳入联合调度，处于立项设想。

---

## 第 7 页｜研究点 1（进行中）

# 会话状态虚拟化：以闲置传输带宽无损扩展等效显存容量

右侧示意图 `figure-rp1-idea.png`：GPU 显存 / 主机内存两层，按 tick 预取↑计算→释放↓，「迁移按时间表、与计算重叠」。

- **研究问题**：并发会话的上下文状态持续增长，显存容量先于计算能力成为瓶颈；会话因显存耗尽而失败时，GPU 利用率仍处于低位。
- **关键洞察**：交互负载具有严格的周期性：每路会话下一周期的计算时刻与所需状态可精确预知，这使状态迁移可以按时间表调度。
- **技术路线**：将近期不活跃的上下文状态存放于主机内存，按交互周期提前预取回显存、用后即时释放；迁移经独立传输通道进行，与计算完全重叠，不占用实时时限。
- **预期与进展**（强调）：瓶颈与方案收益均已由真机实验验证（下页）；上下文无损——现有方案依赖有损截断。

### 怎么讲

> 核心洞察是交互负载的严格周期性，使状态迁移可以按时间表调度：不活跃的上下文状态放在主机内存，按周期预取、用后释放，迁移与计算重叠。瓶颈和方案收益都已经在真机上验证，下一页看实验结果；关键差异是上下文无损，现有方案靠截断历史换容量。

---

## 第 8 页｜研究点 1 · 实验验证

# 同负载公平对照：驻留行为的差异与来源

主体（左，约 62% 宽）：两张白盒时间线截图上下叠放（`rp1-baseline-kv-crop.png` / `rp1-conveyer-kv-crop.png`，8 路会话 KV 驻留实测）：
- 上：**基线——KV 全程常驻，随会话时长单调增长**
- 下：**本方案——仅计算窗口驻留、用后即时释放：以带宽换显存，瞬时占用最小化**

右侧说明卡（三段）：
- **收益来源**：会话状态全量仍随时长增长，但常驻显存的部分被压缩为正在计算的工作集：交互周期严格可预知，状态按时间表预取回显存、用后立即释放，增长的尾部驻留主机内存，搬运与计算完全重叠。
- **量级（初步实验）**：稳态驻留约为基线的三分之一；交付质量零恶化，上下文无损。
- **实验设置与边界**：vLLM 0.23 + Qwen2.5-Omni-7B，RTX 3090，2 秒周期，8 路会话；消费级 GPU 初步实验，验证机制与洞察，生产容量待正式扫描。

### 怎么讲

> 这一页用真实的白盒时间线说话。上图基线：八路会话的 KV 驻留全程常驻、一路单调上涨——会话活得越久占得越多，显存注定耗尽。下图本方案：每路只在自己的计算窗口驻留，用完立刻释放，锯齿有界。收益来源不是魔法：交互周期严格可预知，状态搬运可以排进时间表、藏进计算。量级上稳态驻留约为基线三分之一——初步实验看量级，不做承诺；交付零恶化、上下文无损。

## 第 9 页｜研究点 2（进行中）

# 面向周期性交互负载的静态执行特化

右侧示意图 `figure-rp2-idea.png`：上「通用负载：形状不可枚举」（长度任意 + 批次组合爆炸 → 引擎只把「无新输入的步」静态化）；下「双工负载：形状是物理常数」（输入恒定 = 帧率 × 周期时长，错开相位后签名可枚举 → 整步静态计划、逐周期重放——比通用引擎激进得多的特化）。核心回答「为什么现有方法不能照搬、为什么我们可以更激进」。

- **研究问题**：交互周期内的有效计算量小、执行形状高度重复；通用引擎将每个周期视为全新任务，调度、内核启动与内存规划等固定开销按周期重复支付，占比显著。
- **关键洞察**：双工负载的周期任务输入是固定形状：每周期输入长度由帧率与周期时长决定、恒定不变，批次组成亦可预知，执行形状可归约为有限模板族——而通用引擎按「有无新输入」分类执行路径，结构性看不见这类稳定性。
- **技术路线**：对每类模板预先构建静态执行计划（整步计算图捕获、注意力元数据固化、工作区预分配），运行时直接重放，以消除重复的动态决策开销；形状失配时自动回退通用路径。
- **预期与进展**（强调）：问题已由真机执行剖面量化（下页）；方案分层设计完成，对照实验进行中。

### 怎么讲

> 关键在于双工负载的输入形状是固定的：每个周期进多少输入由帧率和周期长度决定，一成不变，执行形状只有有限几种。但通用引擎按「这一步有没有新输入」来选执行路径，结构性看不见这种稳定性——它的优化机制在我们的负载上系统性落空，这是真机剖面量化过的。思路是为每种形状预编译静态执行计划，运行时直接重放，失配就回退，不影响正确性。

---

## 第 10 页｜研究点 2 · 实验验证

# 执行剖面实测：收益从哪里来

主体：全宽原因图 `figure-rp2-evidence.png`——上条「现状（实测）」：周期条内 8 个橙描边关键步（未图化，约 3 倍于已图化常规步）+ 密排常规步，尾部空隙≈0；下条「特化后（推导）」：关键步收缩，尾部腾出绿色虚线预算块「腾出的时间预算 → 后台结果写回」；底部一句话「收益来源：关键步落在引擎图化机制的覆盖之外——输入形状固定，本可规划一次、周期重放」。

图下两行：
- **实测发现**：每周期的关键路径步恰好未被引擎图化机制覆盖，耗时约为已图化步的 3 倍；周期几乎被执行占满，后台结果没有可用的写回空隙。
- **预期与边界**（强调）：特化后推导可腾出约四分之一周期，数千 token 结果一至两个周期写回；收益为推导预期，对照实验进行中（消费级 GPU 初步实验）。

### 怎么讲

> 这一页讲研究点二的收益从哪里来。上条是实测周期形状：八个橙描边关键步落在引擎图化机制覆盖之外，每个约三倍于常规步，周期被占满、写回没有空隙。下条是特化后的推导形状：关键步收缩，尾部腾出约四分之一预算，数千 token 结果一两个周期写回。对 JiuwenSwarm 这样的系统，这决定后台干完的活能不能及时说出来。

## 第 11 页｜研究点 3（立项设想）

# 端到端交互链路的联合调度

右侧示意图 `figure-rp3-idea.png`：四节点（网络传输/前台推理/后台智能体/播放）+「统一交互时间轴（共享状态）」竖条 + 刚性/弹性两枚标签。

- **研究问题**：双工把交互变成持续过程：deadline 逐周期重复，网络、推理、后台、播放任何一环的抖动都会周期性撞上时限；各环节只能按最坏情况各自留余量，余量层层叠加，最终由并发容量买单。
- **关键洞察**：链路上唯一的松弛在弹性负载：后台调用与结果写回天然容忍延迟。但其时机横跨传输、引擎、播放三个子系统，任何单一环节都看不到全局——协调只能发生在一层联合调度中。
- **技术路线**：以统一交互时间轴为共享状态：推理感知网络抖动，写回给 deadline 让路，播放同时吸收网络与模型两个方差源。
- **初步观察**（强调）：已有实测实例：单环节的优化会被相邻环节的资源竞争抵消——孤立优化不可行并非假设；将复用研究点 1、2 的实验设施验证。

### 怎么讲

> 研究点三的本质：请求式服务里端到端时延是一次性的，各环节各设缓冲就能应付；双工把交互变成持续过程，deadline 每个周期都来一次，任何环节的抖动都会周期性撞上时限——层层留余量意味着每个环节都为最坏情况买单，代价最终摊到并发容量上。而链路上唯一的松弛在弹性负载：后台调用和写回容忍延迟，但它的时机横跨三个子系统，谁都看不到全局，所以协调只能由一层联合调度来做。实验里已经出现过单环节优化被相邻环节抵消的实例，不是空想；立项后复用前两个研究点的设施验证。

## 第 12 页｜交付物

# 交付物：可直接部署的双工交互推理引擎与接入层

- **推理引擎**（强调）：一个面向双工交互负载的推理服务引擎：在 vLLM 上以增量补丁实现状态虚拟化、静态执行特化与时间轴调度，各机制可独立开关、可消融，随开源社区版本演进。
- **智能体接入层**：统一交互时间轴协议 + 会话接入与后台结果写回接口：JiuwenSwarm 等 agentic 系统无需改动内核即可接入，获得实时双工语音交互能力。
- **评测与观测套件**：双工负载的评测协议与全链路白盒观测：负载生成、公平对照方法、逐周期时间线，实验结论可复现、可审计。
- **研究产出**：系统方向论文与技术报告；按研究点分阶段交付——状态虚拟化、执行特化、联合调度，每项均以真机实验验证收尾。

### 怎么讲

> 最后落到交付物：不是一个概念，是四样具体的东西。核心是推理引擎——在 vLLM 上以增量补丁实现三个研究点的机制，每个机制可独立开关、可消融；配一个智能体接入层，JiuwenSwarm 这样的系统不改内核就能接入获得双工语音能力；评测与观测套件保证所有结论可复现；研究产出按研究点分阶段推进，每一阶段都以真机实验收尾。

---

## 全稿统一说法

- 项目定位：为智能体提供**前台双工交互能力**的推理服务系统；对合作方口径为「实时双工交互底座」——不用「外挂」等弱化表述。
- JiuwenSwarm 仅在第 2 页总览大图与第 12 页交付物（接入层）点名；其余各页保持 agent-agnostic 表述。
- 空白表述的分寸：说「服务侧方案未公开、学术研究刚刚起步」，不说「服务系统尚属空白」。
- 版面纪律：无日期、无页脚水印，仅右下角页码；标题一律为直接陈述论点的完整句或名词短语，不用「从 A 到 B」式叙事标题。
- 专业术语仅保留必要者并即时释义（上下文状态（KV 缓存）仅在第 5 页出现一次；deadline 在第 2/4 页配图中随图释义）。表述基准：系统方向论文的学术中文；不用口语，也不用工业黑话。
- **不出现「单 GPU / 单卡」表述**（实验设置行写「单张 RTX 3090」是设备事实，不作为研究边界表述）；实验设置写实：模型 / 引擎版本 / 显卡 / 周期长度如实写出，但不写并发路数、运行时长，不写外推撞墙时间；实验页统一带「消费级 GPU 初步实验，验证机制与洞察」边界行。
- 实验页数字纪律：**数字不做大字、不进标题**，只在正文做量级表述（约三分之一 / 约 3 倍 / 约四分之一周期），每个数字带出处限定（实测 / 推导预期）；实验页以「证据图 + 收益来源」为主体，观众被原因说服而不是被数字说服；追问时按 `docs/findings.md`（H 系列）与 `.context/ideas/static-specialization.md` 展开。
- 研究框架栏目词统一：问题与挑战（挑战 1/2/3，逐一对应研究点 1/2/3，且与第 2 页大图标注 ①②③ 对应）；研究点页内为 研究问题 / 关键洞察 / 技术路线 + 状态行（左行块 + 右示意图版式）；RP1 实验页 = 左证据截图叠放 + 右说明卡（收益来源 / 量级 / 设置与边界）；RP2 实验页 = 全宽原因图 + 两行结论。
- 研究点 3 的表述立足自身问题分析，**不引用、不提及任何具体在研工作**；若被问「有没有类似工作」，回答口径为「实时通信与推理服务的交叉近来开始受关注，我们的差异点是把后台智能体调用这一弹性负载纳入联合调度」。
- 若被问「与已有工作（Metronome）的关系」：该工作以有损的窗口截断应对显存增长；本方案无损保全上下文，且与其正交、可叠加。
