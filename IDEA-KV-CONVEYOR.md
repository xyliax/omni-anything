# 带宽换显存：尾部 KV 传送带（idea 记录，2026-08-01）

> **溯源注（2026-08-06）**：文中引用的 pilot 模拟器数字（如 KV 可行密度 N=12）之原始数据已随深度清理
> 删除；对应主张现由 E1 真栈证据支撑（含撞墙 slack 闭式、三形态解剖），见 `PAPER-EXPERIMENTS.md`。

*从问题定义到方案收敛的完整记录，含被否掉的中间版本与相关工作对比。配套：`PROBLEM.md`（问题定位）、`STORY.md` §5–6（墙序分析与相关工作矩阵）。*

## 一、问题定义

**双工语音 serving 是显存绑定的，且 GPU 死的时候大部分时间在空转。**证据链：

1. **本仓库实测**（3090/1.7B）：KV 可行密度 N=12，而 deadline 墙在 N=192/208（判据 miss>1%）——显存先绑定一个数量级（EVIDENCE §二 S1）；单会话占空比仅 4–9%。
2. **Metronome 独立实测**（[arXiv:2607.02640](https://arxiv.org/pdf/2607.02640)，Qwen3-Omni-30B FP8 / 96G Blackwell）：vanilla（vLLM-realtime 常驻 KV）在 N=128 于 ~148s 撞显存悬崖——每帧延迟从 2–5ms 一步钉死 1.6s，全员僵死；崩溃时刻 GPU 时间占用仅 ~13–32%（由其每帧延迟与错相设置推得）。原话："memory kills sessions whose compute the GPU could easily carry"。
3. **结构原因**：双工会话的 KV 每拍必被 attend、无轮次间隙可换出重算（重算 4k = 330ms prefill = 自我注入，本仓库实测）；每拍只碰每路状态几次，时间占用上限 ≈ 每拍触碰次数 × (容量/带宽) / 拍长 ≈ 20%（H100、480ms 拍）——**卡是先装不下、远没跑满**。

因此的机会：**GPU 的 HBM 容量是稀缺资源，而拍内的 H2D 带宽和计算时间都大量闲置——能否用闲置资源赎回容量？**

## 二、方案演化（含被否版本，保留否证过程）

### v1（已否）：阈值提交 + 重算

尾部不常驻，每拍 decode 前重新 prefill 尾部、用完即弃；攒满阈值 C 才转常驻。

**否证**：重算按拍频复利——每 token 在提交前被重算 ~C/(2a) 次（a≈10 token/拍，C=2048 时 ~100 次）。合并两墙：N = min(N_comp(C), N_mem(C))，对任意 C 都 ≤ 基线（C 小省不了显存，C 大算力墙先塌；7B/H100 代入：C=512 时 N 从 ~60 **跌到** ~26）。本质：用**每拍复发的引擎步时间**（最稀缺的拍内资源）赎买**一次性的字节存量**，汇率差三个数量级；且每发重算都是付 ~13ms 组成税的混合步（STORY §2、EVIDENCE C2），等于把"重算=自我注入"制度化。

### v2（收敛版）：阈值提交 + 时刻表化 H2D 传送带

尾部 KV 的**母本住主机 DRAM**，每拍按时刻表 DMA 搬入 HBM 暂存、该会话算完即释放；攒满 C 才提交常驻。机制从"重算"换成"搬运"：单价从 ~0.035ms/token 引擎步时间降到 ~2µs/token DMA 时间，且走拷贝引擎、与计算并行、不污染批组成。

## 三、方案细节

### 3.1 符号与假设

| 符号 | 含义 | 基准取值（出处） |
|---|---|---|
| L | 单会话上下文（token） | 16k @10min（26.7 tok/s：Qwen2.5-Omni 音频 25 tok/s + 文本输出；按 Qwen3-Omni 13 tok/s 则 11.4k——**L 不影响收益比例**） |
| b | 每 token KV 字节 | 56 KiB（Qwen2.5-Omni-7B thinker config：2×28 层×4 KV 头×128×2B。本仓库 1.7B 实测 112 KB 反而更大——其 KV 头数 8 是 7B 的两倍，GQA 配置差异，非笔误） |
| M | KV 池容量（token） | 55GB/b ≈ 959k（H100 80G − 权重 ~22GB − 开销） |
| B_link, η | H2D 可用带宽及可用率 | PCIe5 实效 ~40GB/s，η=0.7 → 28GB/s |
| T | 拍长 | 480ms |
| P | **一拍可搬入 token 量** = η·B_link·T/b | 234k |
| C | 提交阈值；尾部均值 X = C/2 | C ≈ 2X\*（下） |
| N | 并发路数 | — |

### 3.2 闭式结果

```
显存约束:  N·(L − X) ≤ M        带宽约束:  N·X ≤ P
最优:      X* = L·P/(M+P)       N* = (M+P)/L
收益比:    N*/N₀ − 1 = P/M = η·B_link·T / M_bytes   ← 模型参数 b 被约掉，纯硬件比值
```

**语义：等效 KV 容量 = M + P——一个 token 的 KV 要么占一个常驻位（花 M），要么每拍花一份搬运配额（花 P），拍的尺度上两种"住法"等价。**

| 平台 × 拍长 | 收益比 P/M | N（L=16k，扣暂存后净值） |
|---|---|---|
| H100+PCIe4，480ms | +10% | 60→66 |
| **H100+PCIe5，480ms** | **+24%（净 ~20%）** | **60→~72** |
| 本仓库 3090+PCIe3（实测 12.33GB/s，`calibration/data/pcie_h2d_bench.json`），480ms | **+83%**（池仅 4.97GB，相对带宽富裕） | 原型验证信噪比最高 |
| GH200 C2C，480ms | +260%（死线墙先到，封顶） | — |
| 任意卡，80ms 拍 | ÷6（不值得做） | — |
| 任意卡，2s 拍（Metronome 口径） | ~+100% | — |

代入示例（PCIe5/480ms/L=16k）：X\*≈3.1k token（180MB/路），C≈6–8k，单路搬运 6.4ms，单路计算 10–30ms。

### 3.3 编排设计（方案的贡献主体）

核心认识：**η 不是硬件给的，是调度挣的**——随机相位的需求聚簇造成链路瞬时尖峰，要么搬运迟到（miss）要么被迫压低 η（收益缩水）。设计目标 = 把 H2D 需求摊平成近恒定速率传送带。四个对象：

1. **相位指派（准入时）**：错相从"刻意的实验设置"（Metronome 如此）升级为**调度资源**——准入把新会话插进最空的相位槽（TDMA 化），同时摊平计算批到达与 DMA 到达两条需求曲线。
2. **H2D 传送带（时刻表调度）**：每路每拍一个搬运作业。释放 = 上拍计算完（**内容冻结点**：帧边界后尾部不再变）；截止 = 本拍计算槽 − τ_lead（~40–60ms，覆盖链路排队 p99；对 6.4ms 的服务时间是 ~10× 余量）。链路 EDF + 令牌桶。周期/相位/大小全部先验已知 ⇒ **周期任务集，可静态验证可调度性**（Liu-Layland 式判据可直接写出）。
3. **暂存与提交**：算完即释放暂存指针（DRAM 有母本）；本拍新生 ~11 token/路 后台写回（全场 ~100MB/s 出向，可忽略）；暂存峰值 N·X·(t_x+t_c+τ_lead)/T ≈ 1.8GB（占池 3%，已计入净收益）。**关键张力**：显存收益 ∝ 预取多晚——提前整拍=零收益，零提前=零容错；TDMA 化把排队近似确定化，允许把 τ_lead 压到收益折损 ≤15%。
4. **链路三级混合关键性**：尾部传送带（硬周期）> 注入 KV 搬运（弹性）> 静默停泊（后台）。η 留的 30% 即后两类与抖动的预算——**链路调度与计算侧的注入安放同构，一套死线感知语义在两种资源上各证一次。**

### 3.4 与其他机制的复合

- **提交语义 × 作废**：注入结果以"未提交"态停在 DRAM，确认说出后才提交——40% 被打断的注入从未进常驻池，作废=DRAM 直接丢（EVIDENCE §三 S2 的 24.2% 死 KV 驻留从源头消灭）。
- **× 静默停泊**：停泊省整路（仅适用 ~30% 静默会话），传送带省尾部（适用全部）——同一链路预算的互补消费者。
- **× KV 量化**：fp8/int4 使 M、P 同乘 2–4，绝对路数翻倍，收益比不变。

## 四、相关工作对比：为什么这块是空的、为什么有价值

| 工作 | 对"KV 装不下"的答案 | 局限（本方案的差异点） |
|---|---|---|
| **Metronome'26** | 砍：W=1024 滑窗 + sink（架构失忆搬到 serving 层） | **有损**（窗外内容不可恢复，与注入驻留冲突——几千 token 结果没说完就滚出窗）；本方案**无损**保全上下文 |
| **LiveServe'26** | 轮级 offload + 语义预取（预测下次使用） | 依赖 turn-based 的**轮间空隙**；双工无空隙、回归时刻无需预测（下拍=上拍+T 精确已知）——本方案把 offload 推进到**帧粒度、全体会话** |
| **vLLM resumable / SGLang streaming sessions** | 常驻机制，无策略（"bounds nothing"，Metronome 语） | 提供机制不提供"谁常驻谁流动"的决策——本方案即该决策 |
| **Kyutai 槽位环形 / Qwen Realtime drop-oldest（480–600s）** | 架构级/API 级失忆 | 有损；产品层证明市场在朝长记忆推 |
| **MoshiRAG（ICML'26）** | 模型层回避：弃 insertive 注入（精度更好）改加法叠加，"to constrain sequence length" | **模型侧为 serving 缺位买单的直接证据**；本方案的提交语义正面接住 insertive 注入 |
| 通用 KV offload（KServe/LMCache 等） | 跨请求缓存/卸载 | 面向 ephemeral 请求与轮间隙；无周期硬死线概念 |

**价值论证**：(1) 无损 +20–24%（PCIe5/480ms）的密度是直接可售商品（语音平台按"线"计价，$8–10/线/月）；(2) 可行性建立在双工负载三个**独有**性质上——周期精确已知、相位可指派、内容按帧冻结——通用 serving 不具备，故这是该 regime 的原生机制而非技巧移植，现有工作无人利用；(3) 它把"死线感知调度"从引擎步扩展到 DMA 队列，与本工作的注入安放构成同一混合关键性问题的双资源实例——机制上独立成块，叙事上强化主线。

## 五、验证计划

1. **模拟器扩展**：加链路资源 + 传送带调度器（标定现成：pinned H2D 12.33GB/s、4k swap 38.1ms、多尺寸 64MB–1GB 平坦——`calibration/data/pcie_h2d_bench.json`，2026-08 GPU3 实测）。
2. **三条对照隔离"摊平的价值"**：全常驻基线 / 随机相位传送带（η 被迫低）/ 指派相位传送带（η≈0.7–0.9）。
3. **唯一需要的真机微基准**：DMA 与 decode 步重叠时的干扰系数（拷贝引擎是否偷计算核带宽）——现有标定推不出的唯一参数。
4. 3090 上原型（收益比 +83%，信噪比最高），H100/PCIe5 外推。

## 六、诚实边界

- 收益 ∝ 拍长：80ms 拍不值得做（+4%）；本方案的甜区是 200ms–2s 拍的 thinker 家族。
- 不解寿命墙：L 增长时收益比不变但绝对 N 照样滑坡——需与停泊/压缩/死 KV 回收组合。
- η=0.7 是工程假设，真实值取决于传送带调度质量——恰是实验要测的东西。
- 干扰系数未标定（验证计划第 3 条）；与 paged KV/CUDA graph 的工程摩擦未评估（vLLM 的 HBM-DRAM offload connector 是现成起点）。
