# External Reference Notes

本目录保存外部规格、综述调研和 landscape reference，不是项目事实层，也不是 EuroSys 论文的 scope、mechanism 或 evidence owner。

这里的具体模型、硬件、并发、容量和产品状态可能随来源或时间变化。它们只能用于外部背景和 closest-work 审计；不得把这些细节复制到核心 human docs、根 `AGENTS.md`、README 或论文 planning 中，除非先完成明确的 owner 更新和 paper-scope 冻结。
