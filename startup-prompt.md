# RP1 问题发现实验:服务器 agent 执行 prompt(2026-07-28)

> 配套设计文档:`rp1-problem-discovery-experiments.md`。本文件为交付给实验服务器 agent 的自包含任务书存档;用户约束:服务器上不起真 serving 服务,走"真实微基准标定+按 baseline 机制离散事件模拟"两段式。设计说明(不在 prompt 内):①S3 三种无调度策略=叙事稿"三翻车"的可执行版;②模拟器 6 条机制全部来自已核实 baseline 行为(vLLM 官方博客/#3344/三轮审计),禁止 agent 自行发明语义;③15% 校验闸门=模拟可信度底线。

---

# 任务:双工前台 + 工具调用回注场景的 serving 问题发现(测量 + 模拟)

## 你的角色与总目标
你是系统研究的实验工程师。目标:**用真实 GPU 微基准 + 忠实于现有 serving 系统机制的离散事件模拟,发现"双工语音前台 + 后台工具调用"(GPT-Live 类产品形态)在共享 GPU serving 下的真实问题**。这是问题发现实验,不是方案验证——如果某个预期问题测不出来,如实报告"不存在/量级可忽略",这和发现问题同等有价值。**禁止编造数据;所有结论必须可追溯到测量文件。**

## 研究故事(背景——帮助你理解每个测量的意义;注意:故事是背景,不是要你证实的结论)
我们在研究一类新系统的 serving 问题。产品形态是 GPT-Live 那样的"会打电话的 AI":**前台**是一个双工语音模型,按 480ms 的拍持续边听边说,错过一拍用户就听到卡顿;**后台**是更强的 agent/工具(跑在别的卡上或是 API 调用),干复杂的活,结果返回后要"注入"前台的对话上下文(由前台模型为这些 token 算 KV),前台才能把答案说出来。成本要求单卡同时撑多路会话。于是同一张卡上出现两种性格相反的计算:**周期性硬活**(每拍几个 token,迟到即事故)和**突发弹性活**(大块注入,可推迟、可切块,还可能被用户一句话作废——语料统计约 40% 对话含打断)。我们的研究假设是:现有 serving 系统没有同时认识这两种活的调度器,会产生真实可测的故障与浪费。**具体假设五条(H1–H5),你的实验对每条都有否决权,证实与证伪同等是正产出**:
- **H1**:现有引擎无 deadline 概念,长注入整段拼回会爆拍(存在阈值 L\*,预估在千 token 量级);
- **H2**:多会话共卡时,一路的注入会波及其他路(跨会话爆炸半径)——即这是共享资源的调度问题,不是单会话的工程问题;
- **H3**:无调度下四指标(错拍率/答案首片延迟/会话密度/作废浪费)存在不可兼得的兑换关系,任何固定策略至少牺牲其一;
- **H4**:作废在现状语义下产生可观浪费与正确性问题(白算的 token、作废 KV 长期驻留、打断后过期内容混入上下文);
- **H5**:混批存在"免费搭载区"(decode 访存受限,掺少量 prefill 几乎不增步时),但注入切块有前缀重读罚金(每块重读一遍长上下文),两者共同决定"怎么灌"的设计空间。

## 相关工作地图(谁做过什么、没人测过什么——解释测量的新颖性,也防止你重新发明已有结论)
- **Sarathi-Serve**:发明 chunked prefill + 混批保 decode 延迟(静态每步 token 预算)。T3 的"免费搭载区边界"就是它的机制在双工拍结构下的定标——该曲线在此 workload 下无人发表。
- **Niyama(ASPLOS'26)**:按批内 decode 的 slack 逐步动态定 chunk 大小——但它是软 SLO(错了降级体验),无硬拍、无作废语义。
- **LiveServe / VoxServe**:按播放进度做请求优先级(软实时,缓冲吸收抖动),不改每步批组成。
- **Metronome**:周期实时 omni serving 的准入控制(AIMD 试探安全并发数)。S2 的"错拍率 vs N"与它对话;**注入在场的版本(S3)无人做过**。
- **vLLM-Omni PR #3344**:工具结果整段拼回实时会话的工程实现(无调度、无 deadline、无作废处理)——S1/S3 的策略 (a) 就是它的现状语义。
- **vLLM-Omni RFC #3745**:社区自己列出的六个结构缺陷(KV 无会话生命周期、"流式"实为清零重算、chunk 晚到掉批[NVIDIA 实测吞吐减半/ITL 翻倍]、无持久 session、每 token 跨进程往返、无话轮组件)。S2 的 under-batching 观察与其对照。
- **kyutai moshi-server**:语音编解码头的 lockstep 槽位批生产系统(400 路 STT/H100)——S2 的"相位对齐"设置对应它的形态;但含不定长 decode 的双工主干无人批。
- **Chronos(2026)**:sporadic 请求+固定策略下的 LLM 可调度性分析(线性步时模型)——周期拍版的判据依赖你的 T1–T4 标定数据。
一句话:机制零件各有主人,但**"硬周期拍 × 可作废注入 × 多会话共卡"这个组合的实测行为没有任何公开数据——你产出的就是第一份**。这既是价值,也是责任:量级报告要保守、可复现。

## 背景(workload 定义,照此建模)
- **前台会话**:全双工语音模型,交互切成固定 **480ms 一拍**。每拍:追加一小片输入(约 8 token 的 micro-prefill)→ 主干 decode m_t 个 token(m_t 内容驱动:说话拍约 2–4,静默拍约 1–2,偶有尾部)→ 轻量语音头合成(本实验可不建模语音头,注明此简化)。**硬约束:每拍计算必须在 480ms 内完成,超时 = 音频断裂 = 正确性故障(记为一次 miss)。** 单卡要同时跑 N 路会话(N 是实验变量)。
- **后台工具调用**:对话中随机触发(每会话泊松到达,速率可调),经延迟 D(1–10s 可调)返回**长度 L 的文本结果**(L ∈ {128, 512, 2048, 8192}),需**拼接进该会话的上下文**——即由前台模型为这 L 个 token 算 KV(一次 prefill),才能被后续 decode 引用。
- **打断/作废**:用户随时可能打断(参考先验:约 40% 的对话含打断重置)。打断时:在飞的工具调用作废、其尚未拼入及已拼入的结果作废。现有系统对此**无任何处理**(照灌、不回收),模拟器按现状建模。

## 模拟器必须忠实的 baseline 机制(vLLM / vLLM-Omni 现状语义,逐条实现,不许自行"优化")
1. **step 循环 + 成员冻结**:引擎逐步执行;每步开始时从等待队列组建本步批(FCFS),发射后成员不可变;
2. **每步 token 预算**:本步 decode token 数 + prefill token 数 ≤ `max_num_batched_tokens`(默认 2048,作为变量扫描);chunked prefill:长 prefill 按预算切块跨步执行;
3. **会话 = 常驻可续请求**:拍间不占调度位,新 chunk 到达时回等待队列重新入批(park/wake);
4. **无 deadline 感知**:调度器不知道拍的存在,纯 FCFS + token 预算;
5. **工具结果整段拼回**(现状 #3344 语义):结果到达即作为一个 prefill 请求进队列,无优先级、无作废检查;
6. **步时由标定曲线驱动**(任务一),不许用拍脑袋常数。

## 任务一:GPU 微基准标定(真实测量,产出模拟器物理参数)
单卡 + 一个 7–8B 级 decoder 模型(如 Qwen 系;用 transformers 或 vLLM 离线引擎的**库调用**,不起服务),充分 warmup、CUDA 同步计时、每点 ≥30 次取分布:
- **T1 decode 步时**:一步时间 vs(批大小 B ∈ {1,2,4,8,16,32} × 上下文长度 ctx ∈ {1k,4k,8k,16k});
- **T2 prefill 耗时**:一段 prefill 时间 vs(token 数 L ∈ {64,…,8192} × 既有上下文 ctx);
- **T3 混批干扰**:同一步里 decode 批掺入 p 个 prefill token(p ∈ {0,64,…,2048}),步时如何增长——找出"免费搭载区间"边界;
- **T4 切块罚金**:L=2048 的 prefill 切成 k 块(k ∈ {1,2,4,8,16,32})在 ctx ∈ {4k,16k} 下的**总**耗时——量化"每块重读上下文前缀"开销。
先验对照(此前 3090 + 8B 模型旧测量,sanity check 用,不一致以新数据为准并标注差异):decode 一步约 51ms、读入平摊约 0.46ms/token、KV 约 150KB/token。

## 任务二:离散事件模拟器
按上述 6 条机制实现(任何语言,建议 Python + simpy 或手写事件循环);步时查 T1–T4 标定表(插值)。**校验**:构造一个可在真 GPU 上直接复现的小场景(如 1 会话 + 1 次 L=2048 注入),模拟时间线与实测时间线误差 <15% 才算通过,校验结果写进报告。

## 任务三:实验矩阵(全部在模拟器上跑,多种子取均值±方差)
- **S1 注入冲击**(1 会话):扫 L,记注入前后每拍完成时间与 miss。问题假设:L 超过某阈值必爆拍。**输出爆拍阈值 L\*。**
- **S2 密度基线**(N 路,无注入):错拍率 vs N(N 扫到错拍率 >1%);拍相位随机 vs 对齐各跑一遍;观察并量化 under-batching 吞吐损失。
- **S3 密度 × 注入(核心)**:中等密度(S2 安全 N 的 70%)+ 泊松工具调用。对比三种"无调度"策略:(a) 整段拼回(现状);(b) 按预算切块 FCFS;(c) 只在引擎空转时灌。每种输出四指标:**错拍率、答案首片延迟(结果开始拼入→该会话下一次开口说该内容的模拟时刻)、有效会话密度、作废浪费 token 数**。重点输出:**爆炸半径**——一路注入造成其他路 miss 的数量与分布。
- **S4 作废现场**:S3 设置 + 打断(40% 先验率),现状语义(照灌不回收)下:白算 token 数、作废 KV 驻留时长、打断后过期内容进入上下文的次数。

## 交付物
1. `calibration/`:T1–T4 原始数据(CSV)+ 拟合曲线图 + 环境记录(GPU 型号、驱动、模型、精度);
2. `simulator/`:代码 + 校验报告;
3. `results/`:S1–S4 数据与图(每图带一句话结论);
4. `FINDINGS.md`:**按"证实的问题 / 证伪的假设 / 意外发现"三栏**写发现报告,每条带量级和数据文件引用。**开头放一张 H1–H5 判决表**(每条假设:证实/证伪/部分/数据不足 + 一句话依据 + 指向图表)。特别回答:注入爆拍阈值多少?跨会话爆炸半径存在吗、多大?四指标兑换曲线长什么样?作废浪费量级?切块罚金随上下文长度恶化到什么程度?

## 纪律
不起任何网络服务;单卡完成;时间预算不够时优先级 T1–T3 > S1 > S3 > S2 > S4 > T4;任何简化(不建模语音头、文本 token 代音频 token 等)显式列在 FINDINGS.md 的"简化与效度威胁"一节。
