\usepackage{xspace}
\usepackage{booktabs}

% Paper-local names. Do not use these macros to introduce new canonical terms.
\newcommand{\systemname}{\textsc{Pilarius}\xspace}

% Visible marker for the scaffold stage. Remove every use before submission.
\newcommand{\draftplaceholder}[1]{%
  \par\noindent
  \fbox{\parbox{0.94\columnwidth}{\textbf{Draft placeholder.} #1}}%
  \par
}

% Placeholder for pending results, figures, and verification-gated text.
% Gray box so reviewers of the scaffold can spot every open slot at a glance.
\newcommand{\placeholder}[1]{%
  \par\medskip\noindent
  \colorbox{black!8}{\parbox{0.94\columnwidth}{\small\textbf{Placeholder.} #1}}%
  \par\medskip
}
