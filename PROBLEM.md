# Problem 定位（收敛版，2026-08）

> **溯源注（2026-08-06）**：本文的问题定义与三要素仍为现行；文中源自模拟器 pilot 的具体数字已被
> E1 真栈数据取代（更强的同向结论），以 `PAPER-EXPERIMENTS.md` 执行记录为准。

*汇集全部实验与调研修正后的最终口径。配套：`STORY.md`（完整叙事与相关工作）、`EVIDENCE.md`（实验证据索引）、`TIMELINES.md`（时间线可视化）、`IDEA-KV-CONVEYOR.md`（收敛的方案候选，未验证）。文档地图见 `README.md`。*

## 一句话

**双工语音 serving 是显存绑定的：KV 每拍必被 attend、常驻单调涨、无轮次间隙可换出，于是 GPU 在时间占用仅 ~13–32% 时就因装不下而死——容量是稀缺资源，拍内的 H2D 带宽和计算时间却大量闲置。我们的回答是用闲置带宽赎回容量：时刻表化的尾部 KV 传送带（`IDEA-KV-CONVEYOR.md`），等效 KV 容量 = M + P，收益是纯硬件比值（PCIe5/480ms 拍净 ~20%）。**

## 负载的三要素

| 要素 | 内容 | 去掉它变成什么 |
|---|---|---|
| ① 硬周期拍 | 每 480ms（谱系 80ms–2s）一次 ~8 token 增量 prefill + 2–4 decode，超时 = 音频断裂 = 正确性事故 | 普通 chat serving（Sarathi/Niyama 已解） |
| ② 弹性注入 | 后台 agent 工具结果，数百–数千 token、~10% 长尾 4–8k、40% 概率被打断作废 | 纯双工锁步（moshi-server/Metronome 已解） |
| ③ 单卡多路 | N 是目标函数（成本约束），不是外生给定 | 单会话工程问题 |

三要素同时在产品层落地：GPT-Live（OpenAI 2026-07，工具调用是拍内决策、后台 GPT-5.5 回注）与 Seeduplex（字节 2026-04，豆包全量）都是①+②+③，serving 均闭源——见 STORY §5(e)。

## 四个实测事实（本仓库 + Metronome 交叉验证）

1. **显存先绑定一个数量级**（S1）：KV 可行 N=12 ≪ deadline 墙 N=192/208（随机/对齐，判据 miss>1%；该墙为模拟外推——真实 unbounded 引擎在 N>12 区间是显存悬崖硬僵死，不是弹性排队，见 `TIMELINES.md` L1b 前提域注）；单会话占空比 4–9%。Metronome 真栈复现：vanilla N=128 显存悬崖时 GPU 时间占用仅 ~13–32%——"memory kills sessions whose compute the GPU could easily carry"。
2. **相位从没被当成资源**（S1）：随机相位比对齐多花 4.96× GPU 时间，组成税占全卡忙时 64.7%；批大小由到达巧合决定，调度器零话语权。
3. **整段注入与拍死线正面冲突，伤害由相位 1:1 决定**（S3）：最坏相位 L\*=6144 打爆死线，拍初 8192 都安全；冲击一拍宽。注入走引擎步就必然与拍抢同一资源。
4. **作废黑洞**（S2）：40% 打断先验下峰值 24.2% 常驻 KV 是死内容、驻留 35.6s、每分钟 2.2 次陈旧拼接（正确性事故）；死字节每步 decode 照付带宽原价。

## 三面墙与可行域

- **显存墙**：KV 池字节 / 单路工作集——vanilla 语义下最先到（本仓库 N=12；Metronome N=128 悬崖，且亚稳、静默）。
- **死线墙**：状态被绑定/换出后才成为绑定约束（Metronome windowed 后 N\*≈209 < 显存外推 ~500；本仓库模拟外推 N=192/208，判据 miss>1%）。
- **注入墙**：效率地板上拍 + 注入总需求 = 1 → **N≈57**（`TIMELINES.md` L1b：baseline N=48 已两头坏，48→57 是调度器空间，>57 是准入控制领域）。

墙序是硬件/负载参数的函数，但**在所有现实配置下显存墙都远早于卡的计算能力耗尽**——这是全部机会所在。

## 领域空白（为什么是现在、为什么没人做）

- **serving 文献**：真双工 GPU serving 截至 2026-08 仅 Metronome 一篇（无注入、无作废、最温顺负载）；其对 KV 的答案是滑窗砍掉（有损，与注入驻留冲突）。
- **模型层在为 serving 缺位买单**：MoshiRAG 明知 insertive 注入精度更好，弃用"to constrain sequence length"；全部开源双工模型上下文折合会话时长 ≤20 分钟——领域对 KV 增长的现行答案是"忘掉"。
- **产品层证词**：Seeduplex 自述克服"高并发下的延迟尖刺与稳定性问题"（解法不公开）；GPT-Live 单会话 ≥1 小时（手段不公开）。
- 五列能力矩阵（拍死线/凑批/KV/注入调度/作废语义）中**"注入的死线感知放置"与"输入侧作废回收"两列全场空白**——恰是本负载要素②的两半。

## 与 Metronome 的关系（本仓库引用最密的外部工作，一表说清）

Metronome（arXiv:2607.02640，2026）是截至 2026-08 唯一的真双工 GPU serving 论文，本仓库与它的关系有六重，性质各不相同——引用时别混：

| # | 关系 | 内容 |
|---|---|---|
| 1 | **交叉验证**（增强本仓库结论） | 它在真栈（Qwen3-Omni-30B FP8 / 96G / 2s 拍）独立测得同一墙序：vanilla 显存悬崖先到（N=128 亚稳崩溃、GPU 时间占用仅 ~13–32%）；KV 绑定后死线墙 N\*≈209 < 显存外推 ~500——与本仓库 S1 的"N=12 ≪ 192/208"形态一致（数值巧合不构成校准） |
| 2 | **对照负载**（划定它没覆盖的） | 它测的是双工 regime 最温顺负载：每帧活恒定、无注入、无作废——本仓库要素②的两半它全空 |
| 3 | **方案种子**（我们从它那里拿走的） | 它的刻意错相实验设置被本方案升级为调度资源（相位指派/TDMA）；它的 windowed-KV 实现的"状态有界世界"正是 TIMELINES L1b 思想实验的现实版 |
| 4 | **竞争方案**（我们与它正面对比的） | 它对"KV 装不下"的答案是 W=1024 滑窗砍掉（**有损**，与注入驻留冲突）；传送带**无损**保全上下文——IDEA §四对比表第一行 |
| 5 | **互补可叠加** | 它管墙外（AIMD 准入）与有损封顶；本方案扩张墙内容量——两者不排斥 |
| 6 | **未来 baseline** | 其开源 harness（vLLM-realtime resumable requests + 共享 tick gateway）是本仓库走出模拟器后新实验的基线参照（EVIDENCE 环境注） |

## 方案：带宽换显存的尾部 KV 传送带（详见 `IDEA-KV-CONVEYOR.md`）

一句话：**每路尾部 KV 母本住 DRAM，按时刻表每拍 DMA 搬入 HBM 暂存、算完即释放；攒满阈值才提交常驻。等效 KV 容量 = M + P，收益比 P/M = η·B_link·T/M_bytes（模型参数被约掉，纯硬件比值）。**

- 为什么可行且只在这个 regime 可行：周期精确已知（下拍=上拍+T）、相位可指派（准入时 TDMA 化）、内容按帧冻结（释放点=上拍算完）——三个性质通用 serving 都不具备。
- 与四个事实的对应：事实 1 是收益来源；事实 2 升级为编排设计第一对象（相位指派摊平计算与 DMA 两条需求曲线）；事实 3 的注入 KV 走同一链路第二优先级、不进引擎步；事实 4 由提交语义从源头消灭（未提交停 DRAM，作废直接丢）。
- 收益：PCIe5/480ms +24%（净 ~20%）、3090/PCIe3 +83%（原型信噪比最高）、2s 拍（Metronome 口径）~+100%；80ms 拍不值得做（+4%）——甜区是 200ms–2s 的 thinker 家族。
- 与 Metronome 正交且互补：它解决墙外（准入）与有损封顶，本方案扩张墙内容量且无损——两者可叠加。

## 明确不做什么（边界）

语音合成头与音频 token 化；过载区 N>墙（准入控制，Metronome 地盘）；多卡 P/D 分离（作 baseline 对比而非贡献）；80ms 锁步家族（收益 ÷6）。效度前提见 `EVIDENCE.md` §六（最重要：标定模型 1.7B，阈值是乐观上界）。
