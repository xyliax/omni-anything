# Problem 定位（收敛版）

*汇集全部实验与讨论修正后的最终口径。配套：`STORY.md`（叙事）、`TIMELINES.md`（证据时间线）、`FINDINGS.md`（原始结论索引）。*

## 一句话

**双工语音前台 + 后台 agent 回注，是第一个把"硬实时周期任务"和"可作废的弹性大作业"塞进同一个 token 级调度器的负载；现有 serving 引擎的每条机制都以"请求无身份、时间无意义"为前提，于是在容量墙 1/7 处就系统性崩坏——且旋钮空间已被排除法扫空：这不是参数问题，是调度模型缺一个维度。**

## 负载的三要素（缺一不可——去掉任何一个，问题退化为已解决问题）

| 要素 | 内容 | 去掉它变成什么 |
|---|---|---|
| ① 硬周期拍 | 每 480ms（谱系 80–500ms）一次 ~8 token 增量 prefill + 2–4 decode，超时 = 正确性事故 | 普通 chat serving（Sarathi/Niyama 已解） |
| ② 弹性注入 | 数百–数千 token、长尾分布（~10% 达 4–8k）、可推迟可切块、40% 概率被打断作废 | 纯双工锁步（moshi-server/Metronome 已解） |
| ③ 单卡多路 | N 是目标函数（成本约束），不是外生给定 | 单会话工程问题（无爆炸半径、无共卡连坐） |

## 现有引擎在此负载下的四类实测故障

1. **跨会话爆炸半径**：一次长尾注入同时打爆 7 路无关会话；真实分布下 10% 长尾即把干净会话从 8/8 打到 1.8/8——稀有事件、共享灾难（长尾 × 密度乘积是真实威胁模型，均值无害）。
2. **步内预算管道饿死**：全场死线最硬、体积最小的 8-token 唤醒，排在无死线的千 token 注入后面等待入场（miss 拍的 480ms 里中位 401ms 花在等门开）；对齐负载下整队连坐（坏周期 43/64 同死）、迟到跨拍级联（miss 链最长 19 拍）。
3. **作废黑洞**：峰值时 24% 常驻 KV 是已作废内容——不只占显存，**每步 decode 都为死字节付带宽原价**；每分钟 2.2 次陈旧内容拼入上下文（正确性事故，模型说出用户已否决的答案）。
4. **假满载**：util=1.0 中约一半是引擎自制浪费（欠批 + 每步组成税），MFU 个位数；运维指标全面误导。

## 为什么必然发生、为什么不是参数问题

**根因一元化**：请求元数据没有身份（死线 / 可延性 / 可作废性），于是七条在聊天负载下各自合理的机制集体反转：从不等待→批靠到达巧合（合并权不在调度器）；FCFS→最贵最不急排在最便宜最急前面；running 优先→半吸收注入每步压死硬死线唤醒；chunked prefill→每块重付 13ms 组成税、摊薄成全员放血；LRU→逐掉 480ms 后必用的；重算恢复→自我注入 330–720ms 巨石；到达即拼→死 KV 照读、作废答案照说。

**排除法完成度**（本工作独有的证据形态）：扫过 策略{整段/切块/空闲喂/混批+预算级优先/fit-check 隔离} × 预算{320–2048} × 块大小{免费区–整段} × 相位{随机/对齐}——**参数空间内 miss 下界 2.5%、或以答案饿死为代价；跨出参数空间（加入时间语义）后 0.00% 且四指标全绿**。两个反直觉硬结果关闭了最常见的反驳：切块加重而非缓解（税按步收，块小 = 交税多，三种策略形态下三次复现）；Sarathi"预算按 SLO 反推"的配方方向被组成税翻转（预算 2048→320，miss 单调恶化至 2 倍）。

## 科学内核：一个可形式化的新调度 regime

这是 **token 级批处理上的混合关键性实时调度**（mixed-criticality scheduling over batched atomic steps），与经典 RT 理论的差异构成研究对象本身：

- **处理器模型新**：执行单元是原子步，步时是批组成的函数——权重项次可加（可摊薄）、KV 项按序列字节相加（不可摊薄）、外加一笔与组成类别绑定的固定税（13ms，实现属性）。三项均已实测标定（残差 <5%）。
- **任务模型新**：硬实时周期小作业（大小已知且极小）+ 软弹性大作业（**大小精确已知**——经典调度理论里"作业大小不可观测"这一最大障碍在此负载天然不存在）+ 作废语义（沉没成本以 KV 字节形式常驻并持续计税）。
- **关键不可能性（现有引擎上，实测构造）**：注入需要宽度 ≥ 一个 chunk 步（20–190ms）的"无毒存放位"；随机相位提供的位宽 = 480/N，故 N≥3 起大块无处安放、N≥24 起小块也无处安放——**任何策略在"拍"与"答案"两轴必失其一**；凑批是唯一能制造 330–400ms 位宽的手段，因此是承重墙而非优化。此结论显式依赖组成税的存在（税→0 的未来引擎上降级为效率问题）——**结论对实现的依赖边界已划清**。
- **可行域有解析式**：摊薄平点（N≳32）、注入墙（gap(N) ≥ 注入需求(N)×margin，本负载 ≈57–70）、KV 墙（字节/带宽账，哪面先到是硬件参数的函数：3090 上 KV 先绑定、80G 卡上注入先绑定）。墙内是调度的领地，墙外交给准入控制。

## 研究价值与空白（为什么是现在、为什么是我们）

1. **量化的反事实上界**：同负载同硬件，三腿策略（凑批窗口 + 步内预算保留 + defer/容量预定）实测 0.00% miss、48/48 干净、失效形态优雅退化为答案尾延迟——**naive 崩坏点 N≈8 与容量墙 N≈57–70 之间 7× 的密度空间，两头都有测量钉死**。
2. **领域正在围拢但空白仍在**：2026 上半年 Metronome（有界 KV+准入）、LiveServe（swap+语义预取）、VoxServe（流式调度）相继出现——问题被同行验证；但对照五列能力矩阵（拍死线/凑批/KV/注入调度/作废语义），**"注入的死线感知放置"与"输入侧作废回收"两列全场空白**，而这两列恰是本实验矩阵的核心测量对象。
3. **所需的全部信息物理上可得**：注入大小已知、前缀成本可查表、拍周期精确可预测、作废信号即时——缺的只是引擎数据结构里的三个字段和围绕它们的四个决策（凑批窗口、预算保留、defer+预定、语义 KV 停泊/回收），每个决策的收益均已在模拟器上构造性验证。

## 明确不做什么（边界）

超过死线的不可分割整段（机制问题，切块解决，非调度）；过载区 N>墙（准入控制，Metronome 地盘）；语音合成头与音频 token 化；多卡 P/D 分离（作为 baseline 对比而非贡献）。效度前提沿用 `FINDINGS.md` 第五节（1.7B 乐观上界、共享卡、KV 溢出只记录）。
